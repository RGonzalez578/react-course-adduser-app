# Add user example app

## Requirements

Before run this frontend project, make a docker compose of this api locally:
[Course ReactJs API](https://github.com/gemme/reactjs-costa-rica-api.git)

Run project:

```shell
npm install
npm run dev
```
